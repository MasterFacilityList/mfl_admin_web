machine:
    node:
        version: 0.10.33


dependencies:
    override:
        - npm install
        - bower install:
            environment:
                PATH: "$(npm bin):$PATH"


test:
  override:
    - grunt test:
        environment:
            PATH: "$(npm bin):$PATH"
