machine:
    node:
        version: 0.10.33

    environment:
        PATH: "$(npm bin):$PATH"


test:
    override:
        - 'case $CIRCLE_NODE_INDEX in 0) "export RUN_SAUCE_TESTS=true && grunt test" ;; 1) grunt test ;; esac':
            parallel: true
